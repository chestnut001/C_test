#define _CRT_SECURE_NO_WARNINGS 1

#include<stdio.h>
#include<math.h>

int is_prime(int n)
{
	int count = 0;
	for (int i = 2; i <= sqrt(n); i++)
		if (n % i == 0)
			return 0;
	return 1;
}


//二分查找
int main()
{
	for (int i = 100; i <= 200; i++)
	{
		if (is_prime(i) == 1)
			printf("%d  ",i);
	}	
}
